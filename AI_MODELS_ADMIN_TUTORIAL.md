# 📚 AI Models 管理后台详细教程

## 📋 目录

1. [访问 AI Models 管理页面](#1-访问-ai-models-管理页面)
2. [创建新 AI 模型](#2-创建新-ai-模型)
3. [配置基本信息](#3-配置基本信息)
4. [设置 Parameters Schema](#4-设置-parameters-schema)
5. [参数类型详解](#5-参数类型详解)
6. [完整示例](#6-完整示例)
7. [编辑和删除模型](#7-编辑和删除模型)
8. [常见问题](#8-常见问题)

---

## 1. 访问 AI Models 管理页面

### 步骤：

1. **登录管理员账户**
   - 确保你的账户已设置为管理员角色（`app_metadata.role = 'admin'`）
   - 参考 `QUICK_ADMIN_SETUP.md` 设置管理员权限

2. **导航到 AI Models 页面**
   - 访问：`http://localhost:3000/admin/ai-models`
   - 或在 Admin Dashboard 点击侧边栏的 **"AI Models"** 链接

3. **查看模型列表**
   - 页面会显示所有已配置的 AI 模型
   - 可以按类型（IMAGE/VIDEO）和提供商（FAL/REPLICATE）过滤
   - 可以搜索模型名称

---

## 2. 创建新 AI 模型

### 步骤：

1. **点击 "Create Model" 按钮**
   - 位于页面右上角

2. **填写基本信息**（在 "Basic Info" 标签页）

---

## 3. 配置基本信息

### 必填字段：

#### **Name（模型名称）**
- **说明**：显示在前端用户界面的模型名称
- **示例**：
  - `Flux Pro`
  - `Kling 2.1 Master`
  - `Veo 3.1 Fast`
- **建议**：使用清晰、描述性的名称

#### **Type（类型）**
- **选项**：
  - `IMAGE` - 图片生成模型
  - `VIDEO` - 视频生成模型
- **说明**：决定模型在前端显示的位置（图片生成器 vs 视频生成器）

#### **Provider（提供商）**
- **选项**：
  - `FAL` - Fal.ai 服务
  - `REPLICATE` - Replicate 服务
  - `CUSTOM` - 自定义提供商
- **说明**：决定后端调用哪个 API

#### **API Path（API 路径）**
- **说明**：提供商特定的模型路径
- **FAL 示例**：
  - `fal-ai/flux-pro`
  - `fal-ai/flux/dev`
  - `fal-ai/kling-video/v1/standard/text-to-video`
- **Replicate 示例**：
  - `kling-ai/kling-video-v2`
  - `google/veo-3.1-fast`
  - `stability-ai/stable-diffusion`
- **如何查找**：
  - **FAL**: 查看 [Fal.ai Models](https://fal.ai/models)
  - **Replicate**: 查看 [Replicate Models](https://replicate.com/explore)

#### **Cost per Generation（每次生成成本）**
- **说明**：用户使用此模型生成内容时扣除的积分
- **示例**：
  - 图片模型：`4` - `22` 积分
  - 视频模型：`160` - `200` 积分
- **建议**：根据实际 API 成本设置，留出利润空间

### 可选字段：

#### **Description（描述）**
- **说明**：模型的详细描述，帮助用户了解模型特点
- **示例**：
  ```
  High-quality image generation with advanced prompt understanding. 
  Best for photorealistic images and artistic styles.
  ```

#### **Is Active（是否激活）**
- **说明**：控制模型是否在前端显示
- **用途**：
  - 临时禁用某个模型（不删除）
  - 测试新模型时先设为 inactive

---

## 4. 设置 Parameters Schema

**Parameters Schema** 是 AI Models 管理的核心功能。它定义了前端生成器节点中显示哪些参数控件。

### 访问 Schema Editor

1. 在创建/编辑模型对话框中，切换到 **"Schema Editor"** 标签页
2. 你会看到：
   - **左侧**：Monaco JSON 编辑器（代码编辑）
   - **右侧**：实时预览（可视化预览）

### Schema 结构

`parameters_schema` 是一个 JSON 数组，每个元素定义一个参数：

```json
[
  {
    "key": "参数键名",
    "label": "显示标签",
    "type": "参数类型",
    // ... 其他配置
  }
]
```

---

## 5. 参数类型详解

### 5.1. Select（下拉选择）

**用途**：提供多个选项供用户选择

**Schema 结构**：
```json
{
  "key": "duration",
  "label": "Duration",
  "type": "select",
  "options": [
    {"label": "5s", "value": "5s"},
    {"label": "10s", "value": "10s"},
    {"label": "15s", "value": "15s"}
  ],
  "default": "5s"
}
```

**字段说明**：
- `key`: 参数键名（后端接收的字段名）
- `label`: 前端显示的标签
- `type`: 必须是 `"select"`
- `options`: 选项数组，每个选项包含：
  - `label`: 显示文本
  - `value`: 实际值（字符串或数字）
  - `desc`: 可选，描述文本
- `default`: 默认选中的值

**前端效果**：显示一个下拉菜单

---

### 5.2. Grid Select（网格选择）

**用途**：以网格形式显示多个选项（常用于宽高比选择）

**Schema 结构**：
```json
{
  "key": "aspect_ratio",
  "label": "Aspect Ratio",
  "type": "grid_select",
  "options": [
    {"label": "16:9", "value": "16:9", "desc": "1080p"},
    {"label": "1:1", "value": "1:1", "desc": "Square"},
    {"label": "9:16", "value": "9:16", "desc": "Portrait"}
  ],
  "default": "16:9"
}
```

**字段说明**：
- 与 `select` 相同，但 `type` 为 `"grid_select"`
- `options` 中的 `desc` 会显示在选项下方

**前端效果**：显示一个网格布局的选择器（类似 Instagram 的宽高比选择器）

---

### 5.3. Slider（滑动条）

**用途**：在数值范围内选择（如强度、步数等）

**Schema 结构**：
```json
{
  "key": "guidance_scale",
  "label": "Guidance Scale",
  "type": "slider",
  "min": 1,
  "max": 20,
  "step": 0.5,
  "default": 7.5
}
```

**字段说明**：
- `key`, `label`, `type`: 同上
- `min`: 最小值（必填）
- `max`: 最大值（必填）
- `step`: 步进值（可选，默认 1）
- `default`: 默认值（数字）

**前端效果**：显示一个滑动条，可以拖动选择数值

---

### 5.4. Switch（开关）

**用途**：布尔值开关（开启/关闭某个功能）

**Schema 结构**：
```json
{
  "key": "enable_upscale",
  "label": "Enable Upscale",
  "type": "switch",
  "default": false
}
```

**字段说明**：
- `key`, `label`, `type`: 同上
- `default`: 默认值（`true` 或 `false`）

**前端效果**：显示一个开关按钮

---

### 5.5. Text（文本输入）

**用途**：文本输入框（如负面提示词、自定义参数）

**Schema 结构**：
```json
{
  "key": "negative_prompt",
  "label": "Negative Prompt",
  "type": "text",
  "placeholder": "Enter negative prompt...",
  "maxLength": 500,
  "default": ""
}
```

**字段说明**：
- `key`, `label`, `type`: 同上
- `placeholder`: 占位符文本（可选）
- `maxLength`: 最大长度（可选）
- `default`: 默认值（字符串）

**前端效果**：显示一个文本输入框

---

## 6. 完整示例

### 示例 1：图片生成模型（Flux Pro）

```json
[
  {
    "key": "aspect_ratio",
    "label": "Aspect Ratio",
    "type": "grid_select",
    "options": [
      {"label": "21:9", "value": "21:9", "desc": "1568×672"},
      {"label": "16:9", "value": "16:9", "desc": "1456×816"},
      {"label": "4:3", "value": "4:3", "desc": "1232×928"},
      {"label": "3:2", "value": "3:2", "desc": "1344×896"},
      {"label": "1:1", "value": "1:1", "desc": "1024×1024"},
      {"label": "9:16", "value": "9:16", "desc": "816×1456"},
      {"label": "3:4", "value": "3:4", "desc": "928×1232"},
      {"label": "2:3", "value": "2:3", "desc": "896×1344"}
    ],
    "default": "3:4"
  },
  {
    "key": "guidance_scale",
    "label": "Guidance Scale",
    "type": "slider",
    "min": 1,
    "max": 20,
    "step": 0.5,
    "default": 7.5
  },
  {
    "key": "num_inference_steps",
    "label": "Inference Steps",
    "type": "slider",
    "min": 1,
    "max": 50,
    "step": 1,
    "default": 28
  },
  {
    "key": "enable_upscale",
    "label": "Enable Upscale",
    "type": "switch",
    "default": false
  }
]
```

### 示例 2：视频生成模型（Kling 2.1）

```json
[
  {
    "key": "duration",
    "label": "Duration",
    "type": "select",
    "options": [
      {"label": "4s", "value": "4s"},
      {"label": "5s", "value": "5s"},
      {"label": "6s", "value": "6s"},
      {"label": "8s", "value": "8s"}
    ],
    "default": "5s"
  },
  {
    "key": "aspect_ratio",
    "label": "Aspect Ratio",
    "type": "select",
    "options": [
      {"label": "16:9", "value": "16:9", "desc": "1080p"},
      {"label": "9:16", "value": "9:16", "desc": "1080p"}
    ],
    "default": "16:9"
  },
  {
    "key": "motion_scale",
    "label": "Motion Scale",
    "type": "slider",
    "min": 0,
    "max": 10,
    "step": 0.1,
    "default": 5.0
  }
]
```

### 示例 3：高级图片模型（带负面提示词）

```json
[
  {
    "key": "aspect_ratio",
    "label": "Aspect Ratio",
    "type": "grid_select",
    "options": [
      {"label": "1:1", "value": "1:1"},
      {"label": "16:9", "value": "16:9"},
      {"label": "9:16", "value": "9:16"}
    ],
    "default": "1:1"
  },
  {
    "key": "negative_prompt",
    "label": "Negative Prompt",
    "type": "text",
    "placeholder": "Enter what you don't want in the image...",
    "maxLength": 500,
    "default": ""
  },
  {
    "key": "seed",
    "label": "Seed",
    "type": "text",
    "placeholder": "Leave empty for random",
    "default": ""
  },
  {
    "key": "safety_tolerance",
    "label": "Safety Tolerance",
    "type": "slider",
    "min": 1,
    "max": 5,
    "step": 1,
    "default": 2
  }
]
```

---

## 7. 编辑和删除模型

### 编辑模型

1. **在模型列表中**，点击要编辑的模型行
2. **或点击** "Edit" 按钮（如果显示）
3. **修改信息**：
   - 可以修改所有字段
   - Schema 会实时验证
4. **点击 "Save"** 保存更改

### 删除模型

1. **在模型列表中**，找到要删除的模型
2. **点击 "Delete" 按钮**（通常需要确认）
3. **确认删除**

⚠️ **注意**：删除模型是永久性的，建议先设为 `is_active: false` 测试一段时间。

### 批量操作

- **激活/停用多个模型**：选中多个模型，使用批量操作按钮
- **导出模型配置**：可以导出 JSON 用于备份或迁移

---

## 8. 常见问题

### Q1: Schema 验证失败怎么办？

**错误示例**：
```
Schema validation failed: options: Options are required for select and grid_select types
```

**解决方法**：
- 检查 `select` 或 `grid_select` 类型的参数是否包含 `options` 数组
- 确保 `options` 数组不为空
- 确保每个 option 都有 `label` 和 `value` 字段

### Q2: 如何查找正确的 API Path？

**FAL.ai**：
1. 访问 https://fal.ai/models
2. 搜索模型名称
3. 查看模型详情页的 "Endpoint" 字段
4. 示例：`fal-ai/flux-pro`

**Replicate**：
1. 访问 https://replicate.com/explore
2. 搜索模型名称
3. 查看模型页面的 URL，格式为：`https://replicate.com/{owner}/{model}`
4. API Path 就是 `{owner}/{model}`
5. 示例：`kling-ai/kling-video-v2`

### Q3: 参数值如何传递给后端？

**流程**：
1. 用户在生成器节点中选择参数值
2. 前端将参数值存储在 `parameters` 对象中
3. 生成时，前端发送 `{ model_id, parameters }` 到后端
4. 后端从 `ai_models` 表获取 `api_path` 和 `provider`
5. 后端将 `parameters` 传递给相应的 API（FAL/Replicate）

**示例**：
```javascript
// 前端发送
{
  model_id: "uuid-here",
  parameters: {
    aspect_ratio: "16:9",
    duration: "5s",
    guidance_scale: 7.5
  }
}

// 后端处理
// 1. 查询 ai_models 表获取 api_path
// 2. 调用 FAL/Replicate API，传递 parameters
```

### Q4: 如何测试新模型？

**步骤**：
1. **创建模型**，先设置 `is_active: false`
2. **填写 Schema**，使用实时预览检查
3. **保存模型**
4. **在数据库中手动激活**（或通过界面）
5. **在前端测试**：
   - 打开无限画布
   - 创建生成器节点
   - 检查新模型是否出现在列表中
   - 检查参数控件是否正确显示
6. **测试生成**：
   - 选择新模型
   - 设置参数
   - 点击生成
   - 检查后端日志确认参数传递正确

### Q5: 如何克隆现有模型？

**方法 1：通过界面**（如果实现了）
- 点击模型行的 "Clone" 按钮

**方法 2：手动复制**
1. 编辑现有模型
2. 复制 Schema JSON
3. 创建新模型
4. 粘贴 Schema
5. 修改名称和 API Path

### Q6: Schema 中的值类型是什么？

**字符串值**：
- `duration`: `"5s"`, `"10s"`
- `aspect_ratio`: `"16:9"`, `"1:1"`

**数字值**：
- `guidance_scale`: `7.5`
- `num_inference_steps`: `28`

**布尔值**：
- `enable_upscale`: `true`, `false`

**后端处理**：
- 后端会原样传递这些值给 API
- 确保值与 API 文档要求一致

### Q7: 如何添加自定义参数？

**步骤**：
1. 在 Schema Editor 中添加新的参数对象
2. 选择合适的类型（select/slider/switch/text）
3. 设置 `key`（后端接收的字段名）
4. 设置 `label`（前端显示的标签）
5. 保存并测试

**示例**：添加一个 "Style" 参数
```json
{
  "key": "style",
  "label": "Art Style",
  "type": "select",
  "options": [
    {"label": "Realistic", "value": "realistic"},
    {"label": "Anime", "value": "anime"},
    {"label": "Oil Painting", "value": "oil_painting"}
  ],
  "default": "realistic"
}
```

### Q8: 如何设置合理的积分成本？

**考虑因素**：
1. **API 实际成本**：
   - FAL.ai: 查看定价页面
   - Replicate: 查看模型定价
2. **利润空间**：建议设置 20-30% 的利润
3. **市场竞争**：参考同类产品的定价
4. **用户接受度**：测试不同价格点的用户反应

**示例计算**：
- API 成本：$0.10/次
- 目标利润：30%
- 设置积分：13 积分（假设 1 积分 = $0.01）

---

## 9. 最佳实践

### ✅ 推荐做法

1. **清晰的命名**：
   - 模型名称要描述性强
   - 参数 `key` 使用下划线命名（`aspect_ratio` 而不是 `aspectRatio`）

2. **完整的 Schema**：
   - 为所有重要参数创建 Schema
   - 提供合理的默认值
   - 添加描述性标签

3. **测试优先**：
   - 新模型先设为 `is_active: false`
   - 充分测试后再激活

4. **文档化**：
   - 在 `description` 字段中说明模型特点
   - 在 Schema 的 `desc` 中说明参数含义

5. **版本管理**：
   - 模型更新时保留旧版本（设为 inactive）
   - 使用清晰的命名区分版本（如 `Flux Pro v1.1`）

### ❌ 避免的做法

1. **不要使用无效的 API Path**：
   - 确保 API Path 与提供商文档一致

2. **不要跳过 Schema 验证**：
   - 始终检查 Schema 是否通过验证
   - 使用实时预览检查效果

3. **不要设置过高的积分成本**：
   - 会导致用户流失

4. **不要忽略默认值**：
   - 每个参数都应该有合理的默认值

---

## 10. 高级技巧

### 技巧 1：条件参数

某些参数可能只在特定条件下显示。虽然当前实现不支持，但可以通过以下方式模拟：

```json
[
  {
    "key": "enable_advanced",
    "label": "Advanced Options",
    "type": "switch",
    "default": false
  },
  {
    "key": "seed",
    "label": "Seed",
    "type": "text",
    "placeholder": "Only used when Advanced Options is enabled"
  }
]
```

### 技巧 2：参数分组

使用清晰的标签名称来分组相关参数：

```json
[
  {
    "key": "aspect_ratio",
    "label": "📐 Aspect Ratio",
    "type": "grid_select",
    ...
  },
  {
    "key": "quality",
    "label": "🎨 Quality Settings",
    "type": "slider",
    ...
  }
]
```

### 技巧 3：动态选项

虽然当前不支持，但可以通过定期更新 Schema 来实现：

1. 监控 API 文档变化
2. 更新 Schema 中的 `options`
3. 重新激活模型

---

## 📞 需要帮助？

如果遇到问题：

1. **检查浏览器控制台**：查看是否有 JavaScript 错误
2. **检查后端日志**：查看 API 调用是否成功
3. **验证 Schema**：使用实时预览检查
4. **参考示例**：查看默认模型配置

---

**完成！** 现在你应该能够熟练地管理 AI Models 了。🎉








