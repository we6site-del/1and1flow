# 本地搭建详细指南

## 📋 目录

1. [环境准备](#环境准备)
2. [安装步骤](#安装步骤)
3. [常见问题](#常见问题)
4. [验证安装](#验证安装)
5. [下一步](#下一步)

---

## 环境准备

### 必需软件

#### 1. Node.js (18.0+)

**检查是否已安装**：
```bash
node --version
```

如果显示版本号（如 `v18.0.0`），则已安装。

**Windows 安装**：
1. 访问 https://nodejs.org/
2. 下载"LTS"版本（推荐）
3. 双击安装包，按提示安装
4. 重启终端，运行 `node --version` 验证

**macOS 安装**：
```bash
# 使用 Homebrew
brew install node

# 或下载安装包
# https://nodejs.org/
```

**Linux 安装**：
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 或使用 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18
```

#### 2. Bun (推荐) 或 npm

**Bun 的优势**：
- 安装依赖快 3-5 倍
- 启动速度更快
- 更好的开发体验

**安装 Bun**：

**Windows**：
```powershell
# 使用 PowerShell（管理员）
powershell -c "irm bun.sh/install.ps1|iex"
```

**macOS/Linux**：
```bash
curl -fsSL https://bun.sh/install | bash
```

**验证安装**：
```bash
bun --version
```

**如果 Bun 安装失败**，可以使用 npm（Node.js自带）：
```bash
npm --version
```

#### 3. Git (可选，用于克隆项目)

**检查**：
```bash
git --version
```

**安装**：
- Windows: https://git-scm.com/download/win
- macOS: `brew install git`
- Linux: `sudo apt-get install git`

#### 4. 代码编辑器（推荐）

- **VS Code**: https://code.visualstudio.com/
- **WebStorm**: https://www.jetbrains.com/webstorm/
- 或任何你喜欢的编辑器

---

## 安装步骤

### 步骤 1：获取项目代码

**方法 A：如果你已经有项目文件夹**
```bash
# 直接进入项目目录
cd ai-workflow
```

**方法 B：从 Git 克隆**
```bash
git clone <项目地址>
cd ai-workflow
```

**方法 C：从压缩包**
1. 解压 ZIP 文件
2. 在终端中 cd 到解压后的文件夹

### 步骤 2：安装依赖

打开终端，进入 `ai-workflow` 文件夹：

```bash
cd ai-workflow
```

**使用 Bun（推荐）**：
```bash
bun install
```

**或使用 npm**：
```bash
npm install
```

**安装时间**：
- Bun: 10-30秒
- npm: 1-3分钟

**如果安装失败**，请看[常见问题](#常见问题)。

### 步骤 3：启动开发服务器

```bash
bun run dev
```

或使用 npm：
```bash
npm run dev
```

**成功的输出**：
```
  VITE v7.1.12  ready in 176 ms
  ➜  Local:   http://localhost:5173/
  ➜  Network: http://172.31.6.99:5173/
```

### 步骤 4：打开浏览器

在浏览器中访问：
```
http://localhost:5173
```

**你应该看到**：
- 白色的画布界面
- 左侧工具栏（包含各种节点图标）
- 右侧 AI 聊天面板
- 左上角"登录/注册"按钮

🎉 **恭喜！安装成功！**

---

## 常见问题

### 问题 1：`bun install` 或 `npm install` 失败

**错误示例**：
```
error: Failed to download package
ERR! network timeout
```

**解决方法**：

1. **检查网络连接**
   ```bash
   ping google.com
   ```

2. **使用国内镜像（中国用户）**
   ```bash
   # npm 使用淘宝镜像
   npm config set registry https://registry.npmmirror.com
   npm install

   # 或使用 cnpm
   npm install -g cnpm --registry=https://registry.npmmirror.com
   cnpm install
   ```

3. **清除缓存重试**
   ```bash
   # Bun
   bun pm cache rm
   rm -rf node_modules
   bun install

   # npm
   npm cache clean --force
   rm -rf node_modules package-lock.json
   npm install
   ```

4. **使用代理（如果你有VPN）**
   ```bash
   # 设置代理（示例）
   npm config set proxy http://127.0.0.1:7890
   npm config set https-proxy http://127.0.0.1:7890
   ```

### 问题 2：端口 5173 已被占用

**错误示例**：
```
Port 5173 is in use, trying another one...
```

**解决方法**：

**方法 A：使用其他端口**
```bash
bun run dev -- --port 3000
```
然后访问 `http://localhost:3000`

**方法 B：关闭占用端口的程序**

Windows:
```bash
netstat -ano | findstr :5173
taskkill /PID <进程ID> /F
```

macOS/Linux:
```bash
lsof -i :5173
kill -9 <进程ID>
```

### 问题 3：无法访问 localhost:5173

**可能原因**：
1. 开发服务器未启动
2. 防火墙阻止
3. 浏览器问题

**解决方法**：

1. **确认服务器正在运行**
   - 终端应该显示 "ready in XXX ms"
   - 不要关闭终端窗口

2. **尝试其他浏览器**
   - Chrome
   - Edge
   - Firefox

3. **尝试 127.0.0.1**
   ```
   http://127.0.0.1:5173
   ```

4. **检查防火墙**
   - Windows: 允许 Node.js 通过防火墙
   - macOS: 系统偏好设置 → 安全性与隐私 → 防火墙

### 问题 4：页面显示空白或报错

**解决方法**：

1. **清除浏览器缓存**
   - 按 `Ctrl + Shift + R`（Windows）
   - 按 `Cmd + Shift + R`（Mac）

2. **检查浏览器控制台**
   - 按 `F12` 打开开发者工具
   - 查看 Console 标签
   - 截图错误信息

3. **清除 Vite 缓存**
   ```bash
   rm -rf node_modules/.vite .vite
   bun run dev
   ```

4. **完全重新安装**
   ```bash
   rm -rf node_modules
   bun install
   bun run dev
   ```

### 问题 5：Windows 上命令无法识别

**错误示例**：
```
'bun' is not recognized as an internal or external command
```

**解决方法**：

1. **重启终端/PowerShell**
   - 关闭所有终端窗口
   - 重新打开

2. **检查环境变量**
   - 搜索"环境变量"
   - 确认 Bun 或 Node.js 在 PATH 中

3. **使用完整路径**
   ```bash
   # 查找 bun 位置
   where bun

   # 使用完整路径运行
   C:\Users\<你的用户名>\.bun\bin\bun.exe install
   ```

### 问题 6：权限错误（Permission Denied）

**Linux/macOS 错误示例**：
```
EACCES: permission denied
```

**解决方法**：

1. **不要使用 sudo**
   ```bash
   # 错误
   sudo bun install

   # 正确
   bun install
   ```

2. **修复 npm 权限**
   ```bash
   sudo chown -R $USER:$USER ~/.npm
   sudo chown -R $USER:$USER ~/.bun
   ```

3. **使用 nvm/volta 管理 Node 版本**

---

## 验证安装

运行以下测试确保一切正常：

### 1. 创建第一个节点

1. 点击左侧工具栏的"输入"图标（📝）
2. 在画布上点击
3. 应该出现一个输入节点
4. 在文本框中输入："测试"
5. 文本应该正常显示

### 2. 测试图片上传

1. 在输入节点中点击"上传图片 (0/3)"
2. 选择一张图片
3. 图片应该显示在节点中
4. 计数变为 (1/3)

### 3. 测试节点连接

1. 从输入节点右侧的圆形端口拖出
2. 应该出现"图片"和"视频"选项
3. 点击"图片"
4. 自动创建图片生成节点并连接

### 4. 测试图片生成

1. 在图片生成节点中点击"执行"
2. 等待 2-3 秒
3. 应该显示生成的图片（随机图片）
4. 图片以 2x2 网格显示

### 5. 测试放大预览

1. 点击任一生成的图片
2. 应该全屏显示
3. 显示"继续创作"和"关闭"按钮
4. 点击"关闭"能返回画布

### 6. 打开浏览器控制台

1. 按 `F12`
2. 切换到 Console 标签
3. 执行一次生成
4. 应该看到：
   ```
   生成图片使用的提示词: ...
   参考图片数量: ...
   ```

**如果以上都正常**，✅ 安装成功！

---

## 下一步

### 开始创作

1. **阅读用户指南**
   - 查看 `.same/user-guide.md`
   - 了解所有功能

2. **尝试完整工作流**
   - 输入文字描述
   - 上传参考图片
   - 生成 4 张图片
   - 选择最好的继续创作

3. **探索不同节点**
   - 文本生成节点
   - 视频生成节点
   - 尝试不同的模型和参数

### 配置 AI API（可选）

当前应用使用模拟数据。要使用真实 AI：

1. **复制环境变量模板**
   ```bash
   cp .env.example .env
   ```

2. **编辑 `.env` 文件**
   ```env
   VITE_OPENAI_API_KEY=sk-xxx...
   VITE_ANTHROPIC_API_KEY=sk-ant-xxx...
   ```

3. **重启服务器**
   ```bash
   # Ctrl+C 停止服务器
   bun run dev
   ```

### 开发和调试

1. **查看代码结构**
   - 使用 VS Code 打开项目
   - 熟悉 `src/` 目录结构

2. **修改和热重载**
   - 修改任何文件
   - 保存后自动刷新浏览器

3. **查看日志**
   - 终端显示服务器日志
   - 浏览器控制台显示前端日志

### 获取帮助

遇到问题？

1. **查看文档**
   - `.same/troubleshooting.md` - 故障排除
   - `.same/v39-test-guide.md` - 测试指南

2. **检查浏览器控制台**
   - 按 `F12` 查看错误信息
   - 截图错误并搜索解决方案

3. **重启重试**
   - 停止服务器（Ctrl+C）
   - 清除缓存
   - 重新启动

---

## 快速命令参考

```bash
# 安装依赖
bun install          # 或 npm install

# 启动开发服务器
bun run dev          # 或 npm run dev

# 构建生产版本
bun run build        # 或 npm run build

# 预览生产构建
bun run preview      # 或 npm run preview

# 清除缓存
rm -rf node_modules/.vite .vite

# 重新安装
rm -rf node_modules
bun install
```

---

## 系统要求

### 最低配置
- CPU: 双核 2GHz
- 内存: 4GB RAM
- 硬盘: 500MB 可用空间
- 浏览器: Chrome 90+, Firefox 88+, Edge 90+

### 推荐配置
- CPU: 四核 2.5GHz+
- 内存: 8GB+ RAM
- 硬盘: 1GB+ 可用空间
- 浏览器: 最新版 Chrome/Edge

---

**祝你搭建顺利！** 🚀

如果遇到本文未涉及的问题，请查看主 README.md 或提交 Issue。
